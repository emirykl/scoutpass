{
  "name": "scoutpass",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=22.17.0"
  },
  "packageManager": "npm@11.6.2",
  "scripts": {
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build",
    "dev": "npm run build --workspace backend && npm run dev --workspace frontend",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings=0",
    "test": "npm run test --workspace backend && npm run test --workspace frontend",
    "test:keychain": "npm run test:keychain --workspace backend",
    "test:p2p": "npm run test:p2p --workspace backend",
    "test:wdk:network": "npm run test:wdk:network --workspace backend",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace frontend",
    "wallet:player": "npm run build --workspace backend && npm run wallet:show --workspace backend -- player",
    "wallet:scout": "npm run build --workspace backend && npm run wallet:show --workspace backend -- scout"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@qvac/cli": "^0.8.1",
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.7.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "hyperdht": "6.33.0",
    "pear": "^3.0.0",
    "prettier": "^3.9.5",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.64.0",
    "vitest": "^4.1.10"
  }
}
